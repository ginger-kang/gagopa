import React from 'react';
import styled from 'styled-components';

const AccountContainer = styled.div`
  width: 100%;
  height: 100vh;
`;

const Account = () => {
  return <AccountContainer>계정</AccountContainer>;
};

export default Account;
